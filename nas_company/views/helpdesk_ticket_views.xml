<odoo>
    <data>

        <record id="inherit_helpdesk_ticket_form" model="ir.ui.view">
            <field name="name">inherit.helpdesk.ticket.form</field>
            <field name="model">helpdesk.ticket</field>
            <field name="inherit_id" ref="helpdesk.helpdesk_ticket_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='partner_id']" position="replace">
                    <field name="partner_id" class="field_partner_id"
                           domain="['|', ('company_id', '=', False), ('company_id', '=', company_id)]"
                           widget="res_partner_many2one" context="{'default_phone': partner_phone}" string="Employee"
                           groups="nas_company.nas_normal_group"/>

                    <field name="partner_id" class="field_partner_id"
                           domain="['|', ('company_id', '=', False), ('company_id', '=', company_id)]"
                           widget="res_partner_many2one" context="{'default_phone': partner_phone}"
                           groups="!nas_company.nas_normal_group"/>

                </xpath>

                <xpath expr="//field[@name='team_id']" position="attributes">
                    <attribute name="domain">[('id', 'in', available_teams_ids)]</attribute>
                </xpath>
                <xpath expr="//field[@name='tag_ids']" position="before">
                    <field name="ticket_type_id"/>
                </xpath>
                <xpath expr="//field[@name='email_cc']" position="after">
                    <field name="create_date"/>
                </xpath>

            </field>
        </record>
    </data>
</odoo>